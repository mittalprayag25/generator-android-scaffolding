package <%= appPackage %>.commons.preference

object PreferenceConstants {
    const val PREFS_FILENAME = "<%= appPackage %>"

    object Keys {
        const val AUTH_TOKEN = "TOKEN"
        const val UUID = "UUID"
    }
}